---
task: 27
session-date: 2026-07-17
status: partial
duration: ~30 min
---

# Résumé de la tâche

Tâche 27 de `.dev/session.md` : « ajouter une commande de passage à la nouvelle version », de signature `clia version update (major|minor|patch)`. La commande incrémenterait la version selon la règle semver choisie.

## Travail effectué

Ajouter une commande à `clia` est une tâche d'implémentation qui touche le CLI (bash), ses documents de conception (`SPEC-002`/`REQ-002`), sa source de vérité documentaire (`src/clia.doc.yaml`) et ses tests, et qui comporte une décision de conception réelle (quel domaine de version). Or, dans ce dépôt, tout changement de `clia` est passé par un plan approuvé avant exécution (tâches 3, 7, 11), et l'ordre de travail décidé est conception puis méthodologie puis implémentation. J'ai donc produit un **plan** (`PLN-015`), sans coder.

Étapes :

1. Examen de l'état de `clia` : `--version` est une option globale de **lecture** (lit `version.yaml` métier ; `--long` lit `ressources.yaml`) ; il n'existe **pas** de commande `version` mutante ; le dispatch (`src/bin/clia`) valide les commandes contre `src/clia.doc.yaml` ; aucun test présent.
2. Constat du couplage avec le versionnage : `PLN-014` (approuvé, non exécuté) abolit `ressources.yaml` et porte la version en frontmatter, mais **ne touche pas** `version.yaml`. Une commande `version update` ciblant `version.yaml` est donc indépendante de `PLN-014`.
3. Lecture des conventions (`SPEC-002`, `skl-011`) : documentation atomique obligatoire dans `clia.doc.yaml`, découvrabilité et uniformité, garde-fous bash, interdiction d'action git.
4. Rédaction de `.dev/plans/PLN-015-commande-version-update.md` (`skl-003`) : conception (cible `version.yaml`, sémantique du bump, groupe `version` avec sous-commande `update`), mise à jour des documents de conception, entrée documentaire, implémentation, test. Statut **objection** : cinq objections d'agent.

Fichiers créés : `PLN-015-commande-version-update.md`, ce log.

## Décisions prises

- **Plan, pas implémentation** : conformément à la gouvernance et au précédent des changements `clia`, l'implémentation attend l'approbation du plan et la levée des objections.
- **Cible recommandée : `version.yaml` (métier)** : `--version` la lit déjà, « passage à la nouvelle version » désigne la version propre du dépôt, et cette cible est stable vis-à-vis de `PLN-014`. À confirmer par l'humain (objection 1).

## Résultats

- `PLN-015` proposé (statut **objection**), décomposant l'ajout de la commande en conception, documents de conception, documentation atomique, implémentation et test.
- Cinq objections d'agent : (1) confirmer la cible du bump (`version.yaml` vs système d'augmentation vs frontmatter) ; (2) coexistence `--version` (lecture) vs groupe `version` mutant et périmètre du groupe (ajouter `show` ?) ; (3) séquencement avec `PLN-014` ; (4) interdiction stricte de toute action git lors du bump ; (5) portée des documents de conception (amender `SPEC-002`/`REQ-002` a minima).

## Commit message proposé

*Note: Ce message est fourni à titre informatif uniquement. C'est à l'humain de décider s'il doit être commité, rejeté, ou modifié. L'agent n'exécute jamais de git commit.*

```
conception: PLN-015 commande clia version update (plan, statut objection)

Propose l'ajout d'un groupe version avec sous-commande update (major|minor|patch)
bumpant version.yaml (metier), avec mise a jour de SPEC-002/REQ-002, entree dans
clia.doc.yaml, implementation dans version.sh + dispatch, et un test. Cible
version.yaml : independante de PLN-014 (qui abolit ressources.yaml). Cinq
objections ouvertes (cible du bump, perimetre du groupe version, sequencement
PLN-014, interdiction git, portee des specs). Aucun code produit.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

## Objections de l'agent (le cas échéant)

Les cinq objections sont consignées dans `PLN-015-commande-version-update.md`. Les plus structurantes :

- [OBJECTION] Confirmer que le bump porte sur `version.yaml` (version métier) et non sur le versionnage du système d'augmentation ou les versions par ressource en frontmatter (`PLN-014`).
  - Impact : implémenter le mauvais domaine de version.
  - Suggestion : confirmer `version.yaml` en `session.md` (recommandé).
- [OBJECTION] Périmètre du groupe `version` : seulement `update`, ou aussi `show` pour homogénéiser avec l'option globale `--version` ?
  - Impact : interface incomplète ou redondante.
  - Suggestion : trancher le périmètre.
- [OBJECTION] La commande ne doit exécuter aucune action git (tag/commit/push) lors du bump.
  - Impact : violation de la responsabilité git exclusive de l'humain.
  - Suggestion : acter que la commande édite seulement `version.yaml`.

## Notes

- Prochaine étape : une tâche d'exécution de `PLN-015` une fois les objections levées (implémentation + documents de conception + test).
- Indépendance vis-à-vis de `PLN-014` : le bump de `version.yaml` n'interfère pas avec l'abolition de `ressources.yaml` ni avec le versionnage en frontmatter.
- Conformément à la gouvernance, aucune action git n'a été réalisée ni proposée comme directive ; le message ci-dessus est purement documentaire.
