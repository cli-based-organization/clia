# Ce qui a été fait, tâche 27 — deuxième versement

Écrit après la production de `PLN-005` et le traitement de l'objection. `MET-003` étape 3.

## PLN-005 remplace PLN-004

`PLN-004` passe à `statut-plan: abandonne`, sans que ses chantiers soient réécrits. Il est conservé comme trace de ce qui était planifiable quand une seule réponse existait.

Le type `PLN` est `travail` : le statut évolue, le contenu reste.

## Neuf chantiers

| Réf | Chantier | Dépend de |
|---|---|---|
| **A** | **Corriger la méthode**, deux règles dans `skl-001` | rien |
| B | `ONT-001`, avec le vocabulaire de relations et deux frontières | A |
| C | Ajuster `RES-007`, `RES-010`, `RES-011` | rien pour C1-C2 |
| D | Type `REG` et trois instances : dette, bogues, tâches à faire | B |
| E | Un `PDC` sur la distillation | A, B, **et l'humain** |
| F | Type technote | B |
| G | Le rôle contextuel source ou générée, et sa propagation | mécanisme de génération |
| H | Le cycle de vie collectif, et l'espace actif | B |
| I | Les deux catégories de ressources | rien |

**Le chantier A ne produit aucun document.** Il corrige la méthode que trois réponses mettent en cause. Tant qu'il n'est pas fait, chaque chantier suivant reproduit le défaut.

**Le chantier B en conditionne cinq.** L'ontologie fixe le vocabulaire, et trace les deux frontières demandées : information contre savoir, concept contre relation.

**Le chantier H n'a pas de solution.** Aucune réponse ne dit par quoi remplacer le cycle de vie individuel de `RES-001`. Il est décrit pour que la question ne se perde pas.

**L'ajustement minimal est A, C1 et C2** : deux règles de méthode, et le remplacement du test d'admission de `RES-007` par le critère unique de compatibilité.

## NON-004 est levée

Les sept questions portent une réponse. L'état passe de `ouverte` à `repondue`, l'effet de `conditionnel` à `informatif`.

C'est la troisième objection du dépôt à être levée, après `NON-001` et `NON-002`, sur vingt-huit.

**Ce que Q2 et Q3 ont répondu.** L'objection les désignait comme décisives. Q2 demande de produire `ONT-001`. Q3 écarte la prémisse de la question et redéfinit le savoir comme une relation entre un acteur et une information.

## Ce que ce plan porte contre lui-même

Quatre objections, dont la première me vise.

Le chantier A tire des règles de trois reproches adressés à l'agent, et c'est l'agent qui les tire. `ANL-008` C1 ramène les trois à un seul défaut, ce qui les rend plus faciles à traiter qu'ils ne le sont.

Neuf chantiers pour sept réponses est beaucoup, et `NON-002` conteste le coût du modèle.
