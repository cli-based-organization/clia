# Démarche de validation, tâche 4 de SES-002

1. Les expérimentations antérieures ont été consultées, et ce qui en est repris est nommé.
2. `PLN-009` satisfait les trois contrôles de `PDC-003` : livrable unique, critère exécutable, limite déclarée, pour chacun de ses chantiers.
3. Le total des durées est sous la limite déclarée.
4. Chaque commande demandée est soit un chantier, soit explicitement sortie du plan.
5. Un `ISU` et un `NON` portent ce qui est sorti, comme la demande le prescrit.
6. Les mesures citées sont vérifiables : version, instances de `SPC`, dépôts avec `setup.sh`.
7. `PLN-009`, `ISU-012` et `NON-039` sont conformes à leur schéma.
8. Leurs liens relatifs pointent sur des fichiers existants.
9. Validation de schéma du dépôt entier : le nombre de non conformes n'augmente pas.
10. Le journal suit `MET-003`.
