---
type: log
task: 36
session-date: 2026-07-17
status: partial
duration: 40 min
---

# Résumé de la tâche

Tâche 36 de `session.md`, de type [implementation] : exécuter le plan `PLN-017`. Le plan déclare deux breakpoints ; l'exécution porte donc sur le **segment 1 (Conception)** et s'arrête au **BREAKPOINT 1**, conformément à `CONSTITUTION.md` (« Breakpoint et exécution segmentée »).

## Travail effectué

- **Lecture préalable** : `.dev/session.md` (tâche 36), `PLN-017` v0.2.0 en entier, `CONSTITUTION.md` (cycle de vie d'un plan, règle absolue, breakpoints), `skl-006-adr`, `ADR-004` v0.2.0 (modèle de ressources en vigueur), `ADR-008` (précédent le plus proche : création d'un type de ressource), `ANL-014` (constats et typologie d'acteurs), `.dev/resource-types.yaml`.
- **Fichier créé** : `.dev/adr/ADR-011-ressource-acteur.md` (v0.1.0, statut `Proposé`). Acte le type `ACT` : rôle et non personne ; type autonome plutôt que section d'ADR ou de harnais ; trois catégories (primaire, secondaire, partie prenante hors scène) ; deux portées (méthode, domaine) ; règle « le système décrit n'est pas un acteur de lui-même » ; gabarit complet ; relation `utilise` ; skill `skl-016-acteur`.
- **Fichier créé** : `.dev/adr/ADR-012-ressource-cas-d-usage.md` (v0.1.0, statut `Proposé`). Acte le type `USE` : table des trois questions distinguant `USE`, `REQ` et `SPEC` ; place en amont du `REQ` ; règle d'indépendance aux outils avec son test de conformité ; deux règles d'altitude ; gabarit court ; relations `satisfait` et `realise` ; skill `skl-017-cas-d-usage`.
- **Fichier modifié** : `.dev/resource-types.yaml`, v0.1.0 vers v0.2.0. Ajout des entrées `acteur` (prefix `ACT`, `.dev/acteurs`, séquencé, `skl-016-acteur`, édition `co`) et `usage` (prefix `USE`, `.dev/usages`, séquencé, `skl-017-cas-d-usage`, édition `co`), avec commentaire des champs de frontmatter propres à chaque type. Ajout de trois relations au vocabulaire et d'un commentaire déclarant la forme d'écriture provisoire et la dette associée. YAML revalidé avec `yq` après modification.
- **Fichier modifié** : `.dev/plans/PLN-017-cas-usage-acteurs-tracabilite.md`, v0.2.0 vers v0.2.1, statut `résolu` vers `approuvé`, Changelog de révision 3, et amendement de l'étape 1.3 pour y inscrire la troisième relation.
- **Décision (statut des deux ADR)** : `Proposé` et non `Accepté`. Le BREAKPOINT 1 du plan confie précisément à l'humain la validation du modèle ; les acter avant cette validation viderait le breakpoint de son objet.
- **Décision (écart d'exécution assumé, tracé au plan)** : l'étape 1.3 prévoyait deux relations, `utilise` et `satisfait`. Une troisième a été ajoutée, `realise` (un `PLN` réalise un ou plusieurs `USE`), parce que la relation plan vers usage exigée par R7 et décidée à l'étape 1.2 n'avait aucun porteur dans le vocabulaire existant : `derive-de` et `reference` en auraient donné une transcription approximative et non requêtable.
- **Décision (portée des `USE`)** : la portée méthode ou domaine d'un `USE` est **héritée** de son acteur principal plutôt que redéclarée, pour éviter deux sources de vérité sur la même propriété (`PDC-006`).
- **Limite constatée** : `ADR-008` (précédent utilisé comme modèle de forme) cite encore la typologie à cycles de vie et les ensembles composites d'`ADR-004` v0.1.0, abolis par la v0.2.0. Le vocabulaire abrogé n'a pas été repris dans les deux nouveaux ADR ; la mise à jour d'`ADR-008` lui-même est hors portée de cette tâche.
- **Contrôles effectués** : absence de tiret cadratin et de filet hors frontmatter dans les trois fichiers markdown (les `---` restants sont à l'intérieur des blocs de code des gabarits) ; vérification que toutes les cibles de liens markdown relatifs existent réellement sur le disque.

## Résultats

- Segment 1 de `PLN-017` **exécuté** : deux ADR produits, couche type mise à jour.
- `ADR-011-ressource-acteur.md` v0.1.0, statut `Proposé`.
- `ADR-012-ressource-cas-d-usage.md` v0.1.0, statut `Proposé`.
- `.dev/resource-types.yaml` v0.2.0 : douze types livrables, neuf relations.
- `PLN-017` v0.2.1, statut `approuvé`.
- **Arrêt au BREAKPOINT 1.** Aucun livrable des segments 2 à 4 n'est produit : ni skill, ni amendement de `CLAUDE.md`, ni répertoire `.dev/acteurs` ou `.dev/usages`, ni catalogue. Statut `partial` du présent log pour cette raison.
- Ce que l'humain doit décider pour autoriser la reprise : les deux types et leurs gabarits, la règle de séparation méthode / domaine, la règle d'indépendance aux outils, et la forme provisoire des relations.

## Commit message proposé

*Note: Ce message est fourni à titre informatif uniquement. C'est à l'humain de décider s'il doit être commité, rejeté, ou modifié. L'agent n'exécute jamais de git commit.*

```
PLN-017 segment 1 : types de ressources ACT et USE

Execution du segment 1 de PLN-017, arret au BREAKPOINT 1.

ADR-011 (statut Propose) cree le type acteur ACT : un role et non une
personne, ressource autonome plutot qu'une section de harnais, trois
categories, deux portees (methode generique, domaine propre au depot
hote), et la regle que le systeme decrit n'est pas un acteur de lui-meme.

ADR-012 (statut Propose) cree le type cas d'usage USE : un but d'acteur
atteint de bout en bout, en amont du REQ dans la chaine de conception,
independant des outils qui permettent de l'atteindre (aucun nom de
commande ni code de retour), avec deux regles d'altitude et un gabarit
court.

resource-types.yaml v0.2.0 : entrees acteur et usage, relations utilise,
satisfait et realise, et forme d'ecriture provisoire des relations
declaree avec sa dette.

Les deux ADR restent au statut Propose : leur validation est l'objet meme
du breakpoint.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

## Objections de l'agent

Aucune. Les objections de la révision 1 de `PLN-017` ont été résolues à la tâche 35 ; l'exécution du segment 1 n'en a soulevé aucune nouvelle.

## Notes

- Les répertoires `.dev/acteurs/` et `.dev/usages/` ne sont **pas** créés : ils relèvent du segment 3, après le breakpoint. Les emplacements sont déclarés dans la couche type mais restent vides.
- Les skills `skl-016-acteur` et `skl-017-cas-d-usage` sont référencés par la couche type et par les deux ADR alors qu'ils n'existent pas encore (segment 2). C'est une référence pendante temporaire, assumée par la segmentation du plan et invisible à toute validation mécanique tant que la couche relations n'est pas instanciée.
- `CLAUDE.md` n'est pas amendé : la table des livrables reste à onze lignes alors que la couche type en compte douze. La réconciliation relève de l'étape 2.3, après le breakpoint.
