---
task: 19
session-date: 2026-07-09
status: completed
duration: ~50 min
---

# Résumé de la tâche

Exécuter `PLN-006-cli-clia` : livrer `clia`, le CLI bash déterministe de gestion des sessions et d'inspection des ressources livrables, avec ses documents de conception, son versionnage et les amendements de harnais associés.

## Travail effectué

À l'ouverture, une part importante du plan avait déjà été produite lors d'une session antérieure (documents de conception, code) mais restait inachevée et non journalisée. J'ai fait un état des lieux, vérifié la cohérence de l'existant, puis complété et corrigé les pièces manquantes.

### Vérification et corrections du code `clia`

- **Rendu exécutable** : `chmod +x src/bin/clia` (le fichier n'avait pas le bit exécutable, contraire à SPEC-001).
- **`src/lib/resource.sh`** : correction du parsing de `res ls`. `_res_state` capturait tout le paragraphe explicatif entre parenthèses (ex. `PLN-006` affichait deux lignes) ; il ne retient désormais que le mot-clé d'état (ancré en début de ligne, coupe à la parenthèse). `_res_version` renvoie `-` pour les ressources de travail (PLN, non versionnées, `ADR-004`) et n'extrait plus de bruit dans la prose (ancrage `^[-* ]*version[* ]*:`).
- **`src/bin/clia`** : suppression du second `trap ... EXIT` qui écrasait le trap de nettoyage et ajoutait `[ERR] terminaison anormale` à toute sortie non nulle, y compris une erreur d'usage propre (code 2).
- **`src/lib/session.sh`** : `ses check` couvre maintenant l'intégralité de SPEC-003 (ajout de S3 numérotation croissante des tâches, et S5 absence de filet `---` hors frontmatter), en plus de S1/S2/S4 déjà présents. Cela aligne l'implémentation sur REQ-002-F7.
- **`shellcheck -x`** : propre (seuls des SC1091 « info » subsistent, chemins de source relatifs non suivis par l'analyseur, sans incidence à l'exécution).

### Tests fonctionnels

- Lecture seule sur le dépôt réel : `--help`, `--man`, `--version`, `--config`, `res ls`, `res ls PREFIX`, `ses status`, `ses check`, erreur d'usage (code 2).
- Mutations de session en bac à sable isolé (copie de `src/` + fausse arborescence, sans jamais toucher au `session.md` réel, fichier en édition humaine uniquement) : `ses plan` (séquence `x01`/`x02`), `ses open x01` (promotion + `start-at` + suppression de la source), refus d'ouverture si session active (code 1), `ses check`, `ses close` (archive `SES-<date>-<heure>-<slug>` + `end-at`, suppression de `session.md`), refus de `close` sans session, `ses new`, alias long `session`. Toutes les transitions se comportent comme spécifié.

### Versionnage

- **Création de `version.yaml`** (racine) : version du domaine métier (`0.1.0`), lue par `clia --version`, avec la règle de séparation des domaines de version (`ADR-007`).
- **Mise à jour de `.dev/ressources.yaml`** : ajout de l'ensemble vivant `clia` (0.1.0) et de ses membres ; ajout des documents de conception produits pour ce plan (`ADR-006`, `ADR-007`, `SPEC-002`, `SPEC-003`, `REQ-002`) ; bump `ADR-005` → 0.2.0 et de l'ensemble `documents-de-conception` → 0.2.0 ; bump `CLAUDE.md` et `CONSTITUTION.md` → 0.2.0 et de l'ensemble `harness-files` → 0.2.0 ; bump `version-manifeste` → 0.2.0 ; note de séparation des domaines de version.

### Suppression de l'infra de présentation (tâche 18, objection 7)

- Suppression de `activate` (chargeait `PRESENTATION_ROOT`, lualatex, inkscape, ImageMagick) et du répertoire `scripts/` (génération de slides). Remplacés par `setup.sh` + `src/{bin,lib}` déjà en place.

### Amendements du harnais

- **`CONSTITUTION.md`** : ajout de la section « `clia` : gardien déterministe de l'intégrité » (rôle déterministe, l'agent n'invoque jamais les commandes mutantes de session, commandes d'inspection autorisées) ; correction d'un chemin de log résiduel.
- **`CLAUDE.md`** : ajout du nommage des archives de session `SES-<DATE>-<HEURE_OUVERTURE>-<SLUG>` ; section « Versionnage à deux domaines » (`.dev/ressources.yaml` vs `version.yaml`) ; section « Sessions et `clia` » ; convention de langue (code en anglais, doc en français).

### Clôture du plan

- **`PLN-006`** : statut passé à « exécuté » ; ajout de la liste des sessions-tâches liées.

## Résultats

Livrables produits ou finalisés pour PLN-006 :

- `src/bin/clia`, `src/lib/{common,version,resource,session}.sh`, `setup.sh` : CLI fonctionnel, testé, conforme à SPEC-002/SPEC-003/REQ-002/SPEC-001, `shellcheck` propre.
- `version.yaml` (racine) : version du domaine métier.
- `.dev/ressources.yaml` : manifeste à jour (ensemble `clia`, docs de conception, bumps atomiques).
- `CONSTITUTION.md`, `CLAUDE.md` : amendés (rôle de `clia`, versionnage à deux domaines, sessions, convention de langue).
- `activate` et `scripts/` supprimés.
- `PLN-006` marqué exécuté.

Documents de conception présents et vérifiés (produits en amont, contrôlés pour cohérence) : `ADR-005` (0.2.0), `ADR-006`, `ADR-007`, `SPEC-002`, `SPEC-003`, `REQ-002`, `.dev/templates/session.template.md`.

## Commit message proposé

*Note: Ce message est fourni à titre informatif uniquement. C'est à l'humain de décider s'il doit être commité, rejeté, ou modifié. L'agent n'exécute jamais de git commit.*

```
Exécuter PLN-006 : livrer le CLI déterministe clia

Finalise clia (gestion des sessions et inspection des ressources livrables) :
- corrige le parsing de `res ls`, le trap EXIT parasite, et complète `ses check`
  (S3/S5 de SPEC-003) ; rend src/bin/clia exécutable ; shellcheck propre.
- crée version.yaml (version métier) et met à jour .dev/ressources.yaml
  (ensemble clia, docs de conception, bumps atomiques).
- supprime l'infra de présentation (activate, scripts/).
- amende CONSTITUTION.md (rôle déterministe de clia) et CLAUDE.md
  (versionnage à deux domaines, sessions, convention de langue).
- marque PLN-006 comme exécuté.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

## Objections de l'agent

Aucune objection bloquante. Observations non bloquantes signalées à l'humain :

- [OBSERVATION] Le fichier `.dev/session-X01.md` (vide, `X` majuscule) diverge de la convention `session-x<YZ>.md` (minuscule) d'`ADR-006`. C'est un fichier de session (édition humaine uniquement) ; je n'y ai pas touché. À supprimer ou renommer par l'humain, ou via `clia ses plan`.
- [OBSERVATION] De nombreux skills apparaissent modifiés dans l'arbre de travail sans bump de version correspondant dans le manifeste. Hors périmètre de PLN-006 ; à traiter dans une tâche dédiée de mise à cohérence du versionnage.

## Notes

- Les commandes mutantes de `clia` n'ont été exercées que dans un bac à sable isolé, jamais sur `.dev/session.md` (édition humaine uniquement, `CONSTITUTION.md`).
- Les objections résiduelles A, B, C du plan ont été laissées à leur valeur par défaut (non bloquantes), conformément au plan approuvé.
