---
task: 01
session-date: 2026-07-10
status: completed
duration: ~25 min
---

# Résumé de la tâche

Tâche 1 (catégorie analyse) de la session du 2026-07-10 : analyser le script d'installation du dépôt `../../noumanity-dev/ticket-driven-ai` et décrire ses fonctionnalités de setup, en particulier l'installation. Éclaire l'intention de session « fournir un CLI installable pour un utilisateur ».

## Travail effectué

- **Cadrage du type** : tâche portant sur un existant matériel (un script), donc **analyse de corpus** (`skl-012`, `ADR-001`), et non recherche de fondation.
- **Corpus** : lecture intégrale de `setup.sh` (v0.1.0, 251 lignes) et repérage du rôle de `bin/tda` (binaire installé, ~68 Ko), lecture seule sur le dépôt tiers ; consultation du `README.md` du dépôt cible pour le contexte de `tda`.
- **Plan** : `PLN-007-analyse-setup-installation.md` proposé et exécuté dans la foulée (premier jet, aucune objection bloquante — analyse en lecture seule).
- **Analyse produite** : `ANL-2026-07-10-setup-installation.md` selon `skl-012` (objet, périmètre/méthode, inventaire, constats sur cinq dimensions, synthèse orientée `clia`, portée/péremption).
- **Décision** : aucune référence normative n'étant fournie par la tâche, l'analyse reste descriptive ; la section de synthèse oriente les constats vers l'installation de `clia` sans opérer de confrontation formelle à `SPEC-001`/`REQ-001`.

Fichiers créés (chemins relatifs) :
- `.dev/plans/PLN-007-analyse-setup-installation.md`
- `.dev/analyses/ANL-2026-07-10-setup-installation.md`
- `logs/ia-output/LOG-2026-07-10-task-01.md`

## Résultats

- Description complète des fonctionnalités de `setup.sh` : interface (`-h`, `--help`, `--version`, `--check`, `--uninstall`, install par défaut), utilitaires de journalisation, aide courte/longue.
- Description du **mécanisme d'installation** (focus) : modèle « dev + permanent + local » par ajout d'un bloc marqué `TDA_HOME`/`PATH` dans `~/.bashrc`, sans copie ni build, idempotent et réversible, activé par `source ~/.bashrc`.
- Constats critiques localisés : idempotence aveugle au déplacement du dépôt, désinstallation `awk` dépendante d'une ligne vide, extraction fragile de `TDA_HOME`, couplage exclusif à bash, incohérence documentaire `install`/`init` entre `setup.sh` et `README.md`.
- Cinq recommandations priorisées pour l'installation de `clia`.

## Commit message proposé

*Note: Ce message est fourni à titre informatif uniquement. C'est à l'humain de décider s'il doit être commité, rejeté, ou modifié. L'agent n'exécute jamais de git commit.*

```
Analyse du setup/installation du CLI tda (ANL + PLN-007)

Tâche 1 (analyse) : analyse de corpus du script setup.sh du dépôt
ticket-driven-ai, centrée sur le mécanisme d'installation, pour éclairer
la conception de l'installation de clia.

- PLN-007 : plan d'analyse (exécuté).
- ANL-2026-07-10-setup-installation : description des fonctionnalités de
  setup, du modèle « dev + permanent + local » (bloc marqué dans ~/.bashrc),
  constats de fragilité et 5 recommandations priorisées pour clia.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

## Objections de l'agent

Aucune objection bloquante. Observation non bloquante :

- [OBSERVATION] La tâche ne fournit pas de référence normative. Une confrontation formelle de `setup.sh` (et du futur installeur de `clia`) à `SPEC-001`/`REQ-001` mesurerait les écarts plutôt que de les décrire ; elle mériterait une tâche dédiée si l'humain le souhaite.

## Notes

- Aucune modification hors de ce dépôt : le dépôt cible `ticket-driven-ai` a été lu en seule lecture. Les constats de fragilité sont des observations, pas des demandes de modification de ce dépôt tiers.
- `ANL` et `LOG` sont des ressources point fixe / non versionnées ; `PLN` est une ressource de travail non versionnée. Aucun bump de `.dev/ressources.yaml` n'est requis (voir `ADR-004`).
