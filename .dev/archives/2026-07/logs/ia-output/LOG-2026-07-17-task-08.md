---
task: 08
session-date: 2026-07-17
status: completed
duration: ~35 min
---

# Résumé de la tâche

Tâche 8 de `.dev/session.md` : produire `PLN-012`, un plan permettant de livrer le plus rapidement possible les deux livrables attendus de la session (créer/initialiser un nouveau repo git avec le système d'augmentation ; update/rollback des ressources `clia` dans un repo existant). Le livrable de la tâche est le plan lui-même et ce log ; rien n'a été exécuté au-delà.

## Travail effectué

- Lecture de la source de la demande (`.dev/session.md`) et du harnais de gouvernance (`CLAUDE.md`, `CONSTITUTION.md`, `skl-003-plan-de-travail`, `skl-008-log-ia-output`).
- Cadrage de l'existant : `clia` (dispatch `src/bin/clia`, groupes `res`/`ses`), `SPEC-002`, `REQ-002`, `.dev/ressources.yaml`, ainsi que les plans et fondations liés à l'installation (`PLN-008`, `PLN-009`, `FND-2026-07-10-installateurs-packaging`, les ANL de tâche 1).
- Constat central : aucune commande `init`, `update` ni `rollback` n'existe (ni en conception, ni en code), et `clia` résout aujourd'hui sa racine via `BASH_SOURCE`, donc n'agit que sur son propre dépôt. Les deux livrables imposent d'agir sur un repo cible distinct : c'est le prérequis transversal identifié.
- Rédaction de `.dev/plans/PLN-012-livrables-init-update-rollback.md` selon `skl-003` : intention, contexte, spécification du livrable, un « chemin le plus court » (concevoir une fois le socle d'action sur repo cible, puis livrer `init` en premier, puis `update`/`rollback`), un plan en phases 0→3 respectant l'ordre séquentiel d'`ADR-007`, le versionnage atomique et la vérification.
- Formulation de 5 objections d'agent, chacune avec un défaut proposé pour débloquer l'exécution sans allers-retours.

Décisions prises (par défaut, à valider par l'humain via `session.md`) : modèle « outil installé + cible résolue au runtime » ; source de distribution = arbre installé local (hors ligne) ; `rollback` via instantané géré par `clia` ; frontière méthode/domaine par manifeste d'appartenance explicite. Ordre séquentiel conception→méthodologie→implémentation conservé.

Fichiers créés :
- `.dev/plans/PLN-012-livrables-init-update-rollback.md`
- `logs/ia-output/LOG-2026-07-17-task-08.md`

## Résultats

- `PLN-012` produit, statut **objection** (5 objections d'agent ouvertes, chacune avec défaut proposé). Aucune exécution du plan.
- Log de tâche produit.

## Commit message proposé

*Note: Ce message est fourni à titre informatif uniquement. C'est à l'humain de décider s'il doit être commité, rejeté, ou modifié. L'agent n'exécute jamais de git commit.*

```
plan: PLN-012 livrables init/update/rollback (repo cible)

Produit PLN-012 (statut objection), plan du chemin le plus court vers
les deux livrables de session : `clia init` d'un nouveau repo et
`clia update`/`rollback` des ressources d'augmentation d'un repo
existant. Identifie le prérequis transversal (agir sur un repo cible
distinct de l'arbre d'installation), respecte l'ordre séquentiel
ADR-007, et pose 5 objections d'agent avec défauts proposés.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

## Objections de l'agent (le cas échéant)

Les objections détaillées vivent dans le plan (`PLN-012`, section « Objections de l'agent IA »). En résumé :

- [OBJECTION 1] Racine cible vs `BASH_SOURCE` : les livrables exigent d'agir sur un repo distinct de l'arbre d'installation ; amender `REQ-002-NF2`. Défaut : modèle outil installé + cible au runtime (aligne aussi `PLN-008`).
- [OBJECTION 2] Frontière méthode/domaine : sans manifeste d'appartenance explicite, `update`/`rollback` risquent d'écraser du contenu de domaine ou des fichiers en édition humaine. Défaut : manifeste bloquant + `--dry-run` obligatoire.
- [OBJECTION 3] Mécanisme de `rollback` : git du repo cible vs instantané géré par `clia`. Défaut : instantané `clia`, `rollback` livré après `init`/`update`.
- [OBJECTION 4] Source de distribution : arbre installé local vs release distante. Défaut : arbre local, hors ligne, déterministe.
- [OBJECTION 5] Séquentialité : ne pas coder avant d'amender conception (`SPEC-002`/`REQ-002`, nouvel ADR). Défaut : ordre 1→2→3 conservé, périmètre minimisé.

## Notes

- Phase 0 (recherches) considérée close : les fondations et analyses existantes suffisent, ce qui est un gain de vitesse assumé.
- Ce plan croise `PLN-008` (installeur `setup.sh`) : adopter le modèle par défaut de l'objection 1 tranche aussi l'OBJECTION A restée ouverte dans `PLN-008`.
- Conformément à la gouvernance, aucune action git n'a été réalisée ni proposée comme directive ; le message ci-dessus est purement documentaire.
