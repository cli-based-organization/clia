---
type: log
task: 42
session-date: 2026-07-17
status: partial
duration: 55 min
---

# Résumé de la tâche

Tâche 42 de `session.md`, de type [implémentation] : implémenter `PLN-018`, puis `PLN-019`. `PLN-018` déclare un breakpoint après son étape 1.3 ; l'exécution porte donc sur son **segment 1** et s'arrête là. `PLN-019` n'est pas commencé : sa précondition déclarée est l'exécution complète de `PLN-018`.

## Travail effectué

- **Lecture préalable** : `PLN-018` v0.3.0 en entier, `ADR-010` v0.1.0, `ADR-007`, `ADR-004`, `ANL-015`, `REQ-001` (F4, F7 à F10), `SPEC-001`, `skl-006`, `CONSTITUTION.md`.
- **Fichier révisé** : `.dev/adr/ADR-010-clia-setup-commandes-modes-installation.md`, v0.1.0 vers **v0.2.0**, statut `Proposé`. Quatre décisions amendées (D2, D3, D5, D6), trois conservées (D1, D4, D7 dans son principe), deux ajoutées (D8, D9), question de conception ouverte tranchée.
- **Fichier créé** : `.dev/adr/ADR-013-version-augmentation-et-marque-installation.md` (v0.1.0, `Proposé`). Cinq décisions : coïncidence des versions dans le dépôt source, étiquettes comme versions disponibles, marque d'installation, déduction par frontmatter comme contrôle, reconnaissance d'un dépôt équipé.
- **Fichier créé** : `.dev/adr/ADR-014-contrat-extension-outil.md` (v0.1.0, `Proposé`). Sept décisions : déclaration explicite, conformité CLI, propagation transparente, documentation conditionnant l'exposition, contrat versionné, garde-fous, périmètre restreint aux extensions livrées.
- **Fichier révisé** : `.dev/adr/ADR-007-architecture-systeme-augmentation.md`, v0.2.0 vers **v0.3.0** (conséquence de la décision D1 d'`ADR-013`).
- **Fichier révisé** : `.dev/resource-types.yaml`, v0.2.0 vers **v0.3.0** : section `types_etats` déclarant le fichier de marque d'installation.
- **Fichier révisé** : `.dev/plans/PLN-018-...md`, v0.3.0 vers v0.3.1, statut `résolu` vers `approuvé`, Changelog de révision 4.
- **Décision de méthode (les six points ouverts reçoivent une position motivée)** : `PLN-018` listait six points « à trancher par l'humain au breakpoint ». Un ADR qui écrit « à décider » n'est pas un ADR : il ne trace rien et ne peut pas être révisé. Chaque point reçoit donc une **décision motivée**, avec son alternative écartée et la raison de l'écartement, au statut `Proposé`. Le breakpoint sert à confirmer ou renverser, non à combler des vides.
- **Positions retenues sur les six points** :
  - **surface de commandes** : premier niveau (`clia init`), pas de groupe `setup`. Motif : la tâche 38 l'énumère explicitement et la tâche 41 demande la parité avec le dépôt de référence, dont les commandes équivalentes sont de premier niveau. La mention `clia setup ...` de la tâche 40 est signalée dans l'ADR comme incidente à une objection portant sur un autre sujet, et portée au breakpoint ;
  - **nom de la commande d'amorçage** : `install`. Motif : `init` désigne déjà la commande de couche 2 ; deux sens pour un même mot dans le même système est une source d'erreur durable ;
  - **cible non vide** : refus par défaut, option explicite pour passer outre, jamais d'écrasement silencieux ;
  - **mode multi-utilisateur** : hors portée, avec trois motifs et deux alternatives documentées pour plus tard ;
  - **marqueur de reconnaissance** : la marque d'installation, avec un état intermédiaire « équipé sans marque » qui n'est ni refusé ni accepté en silence ;
  - **copie ou lien** : copie par défaut (cible autonome), lien offert en complément en mode dev, avec son risque nommé (cible qui se casse si l'arbre source est déplacé).
- **Vérification de terrain avant de trancher la surface** : inspection du dépôt `ticket-driven-ai`, dont `bin/tda` déclare `TDA_COPY_FILES=(CLAUDE.md CONSTITUTION.md)` et `TDA_COPY_DIRS=(skills)`. Le binaire n'y est pas copié dans la cible, et ses commandes sont de premier niveau. C'est ce constat qui fonde les décisions D3 et D6.
- **Conséquences appliquées au-delà de la lettre du plan, parce qu'elles en découlent mécaniquement** : l'amendement d'`ADR-007` (le plan le prévoyait à l'étape 1.2) et la déclaration du fichier d'état dans la couche type (le plan ne le mentionnait pas, mais tout emplacement de fichier du système y est déclaré, faute de quoi la source de vérité unique aurait une exception non écrite).
- **Écart de conception corrigé au passage** : la section « Conséquences » d'`ADR-007` citait `.dev/ressources.yaml` comme domaine de version, alors que ce manifeste est aboli par `ADR-004` v0.2.0. La correction est faite dans la même section que l'amendement et signalée dans le texte.
- **Contrôles effectués** : validation du YAML modifié avec `yq` ; existence sur disque de toutes les cibles de liens markdown des trois ADR ; absence de tiret cadratin et de filet hors frontmatter.

## Résultats

- Segment 1 de `PLN-018` **exécuté** : trois ADR produits ou révisés, deux conséquences appliquées.
- `ADR-010` v0.2.0, `ADR-013` v0.1.0, `ADR-014` v0.1.0, tous au statut `Proposé` : leur validation est l'objet du breakpoint.
- `ADR-007` v0.3.0, `.dev/resource-types.yaml` v0.3.0, `PLN-018` v0.3.1 statut `approuvé`.
- **Arrêt au BREAKPOINT 1.** Les segments 2 (exigences et spécifications) et 3 (cohérence) ne sont pas exécutés.
- **`PLN-019` n'est pas commencé.** Sa précondition stricte, déclarée dans son propre contexte, est l'exécution complète de `PLN-018`. La tâche 42 demandait les deux plans ; le second est bloqué par le breakpoint du premier, ce qui est le comportement prévu par `CONSTITUTION.md` (« arrivé au breakpoint, l'agent s'arrête et ne produit aucun livrable des segments suivants tant que l'humain n'a pas autorisé la reprise »). Statut `partial` du présent log pour cette raison.

## Commit message proposé

*Note: Ce message est fourni à titre informatif uniquement. C'est à l'humain de décider s'il doit être commité, rejeté, ou modifié. L'agent n'exécute jamais de git commit.*

```
PLN-018 segment 1 : trois ADR d'installation, arret au breakpoint

ADR-010 v0.2.0 : surface de premier niveau (D3), frontiere lecture/ecriture
pour git avec creation de depot autorisee (D5), paquet distribuable excluant
l'outil (D6), mode multi-utilisateur hors portee (D2), couche 2 invoquant la
couche 1 comme extension (D8), detection d'un depot compatible (D9). La
question de conception laissee ouverte en v0.1.0 est tranchee : la creation
du depot revient a l'outil.

ADR-013 : dans le depot source, la version du domaine metier EST celle du
systeme d'augmentation ; les versions disponibles sont les etiquettes ; un
depot equipe porte une marque d'installation qui fait foi ; la deduction
depuis les frontmatters en est un controle de coherence.

ADR-014 : contrat d'extension versionne. Declaration explicite plutot que
decouverte, conformite aux conventions de flux et de codes de retour,
documentation conditionnant l'exposition, garde-fous de determinisme et de
git, perimetre restreint aux extensions livrees.

Consequences : ADR-007 v0.3.0 (coincidence des versions et sa limite,
correction d'une reference au manifeste aboli), resource-types.yaml v0.3.0
(declaration du fichier d'etat).

Les trois ADR restent au statut Propose : leur validation est l'objet du
breakpoint. PLN-019 n'est pas commence.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

## Objections de l'agent

Aucune objection bloquante. Un point est signalé sans bloquer :

- [SIGNALEMENT] La décision D3 d'`ADR-010` (surface de premier niveau) repose sur deux signaux humains contradictoires : la tâche 38 énumère `clia init`, la tâche 40 écrit `clia setup ...`.
  - Impact : si la position retenue est la mauvaise, elle se propage aux exigences et spécifications du segment 2, puis au dispatch et à la source documentaire dans `PLN-019`. Le coût de correction croît à chaque étape franchie.
  - Suggestion : confirmer ou renverser ce point en priorité au breakpoint, avant tout autre.

## Notes

- Ce que l'humain doit trancher pour autoriser la reprise : les six positions énumérées plus haut, et en priorité la surface de commandes.
- La décision D1 d'`ADR-013` est **contextuelle** : la coïncidence des versions vaut dans ce dépôt et pas dans un dépôt hôte. C'est le risque nommé de cet ADR, et sa limite est répétée aux trois endroits où elle est citée (`ADR-013`, `ADR-007`, `PLN-018`).
- L'état « équipé sans marque » introduit par `ADR-013` (décision D5) n'a aucune commande pour le régulariser dans la portée actuelle : cul-de-sac connu, à ouvrir avec les parcours de version.
- `CLAUDE.md` ignore toujours les types `ACT` et `USE` dans sa table des livrables (étape 2.3 de `PLN-017`, non exécutée).
