---
type: log
task: 43
session-date: 2026-07-17
status: partial
duration: 75 min
---

# Résumé de la tâche

Feedback humain demandant d'employer `clia setup CMD` plutôt que des commandes de premier niveau, d'accepter les propositions par défaut, et d'implémenter `PLN-018` puis `PLN-019`. `PLN-018` est exécuté en entier ; `PLN-019` est exécuté jusqu'à son BREAKPOINT 1.

## Travail effectué

### Décisions (reprise après le breakpoint de `PLN-018`)

- **Fichier révisé** : `ADR-010`, v0.2.0 vers **v0.3.0**, statut `Proposé` vers **`Accepté`**. La décision D3 est **rétablie dans sa forme d'origine** : la couche 2 est exposée par le groupe `clia setup`, non par des commandes de premier niveau. Sous-commandes retenues : `init` et `versions`, plus `upgrade` et `downgrade` réservées.
- **Décision (nom de la sous-commande de versions)** : `versions` plutôt que `version ls`. Aucune commande de l'outil n'a aujourd'hui trois niveaux et `clia setup version ls` en aurait trois ; `versions` reste à un seul niveau et évite la collision avec `--version` et `release`, qui portent sur la version du domaine métier.
- **Fichiers révisés** : `ADR-013` et `ADR-014`, v0.1.0 vers v0.1.1, statut `Proposé` vers **`Accepté`** (les propositions sont acceptées par défaut, conformément au feedback).

### Exigences et spécifications (segment 2 de `PLN-018`)

- **Fichier créé** : `.dev/requis/REQ-003-installation-et-extension.md` (v0.1.0). Vingt-deux exigences fonctionnelles réparties en trois blocs (couche 1, couche 2, contrat d'extension), cinq non fonctionnelles, une section de tensions et une table de traçabilité vers les trois `USE`.
- **Fichier révisé** : `.dev/requis/REQ-002-cli-clia.md`, v0.2.0 vers **v0.3.0** : section « Installation (groupe `setup`) », exigences F13 à F19 (commandes, résolution et reconnaissance de la cible, propagation du code de retour, refus sur contrat incompatible, cohérence dispatch et documentation étendue aux extensions).
- **Fichier créé** : `.dev/specs/SPEC-004-script-amorcage-et-extension.md` (v0.1.0) : invocations, effets, codes de retour, ordre non négociable des opérations d'installation, forme du bloc marqué, et table du contrat d'extension en version 1.0.0.
- **Fichier révisé** : `.dev/specs/SPEC-002-cli-clia.md`, v0.2.0 vers **v0.3.0** : section « Groupe `setup` » avec table d'invocations, table des quatre états de reconnaissance de la cible, règle de délégation, plus quatre lignes de traçabilité.

### Rattachement amont et cohérence (segments 2.3 et 3 de `PLN-018`)

- **Fichiers révisés** : `USE-001`, `USE-002`, `USE-003` (v0.1.0 vers v0.1.1). La relation `satisfait`, qui déclarait qu'aucune exigence ne servait ces parcours, pointe désormais vers les exigences et spécifications produites. La chaîne `ACT` vers `USE` vers `REQ` vers `SPEC` est refermée pour ces trois parcours.
- **Correction d'une erreur de la tâche 41** : le pas 4 du flux nominal de `USE-002` listait encore l'outil parmi ce qui est matérialisé dans la cible. Le log de la tâche 41 affirmait qu'aucun `USE` n'avait eu besoin de correction ; c'était inexact sur ce point. Corrigé ici.
- **Fichier révisé** : `ACT-003` (v0.1.1). Sa définition couvre désormais l'équipement du **poste**, précondition de l'équipement d'un dépôt, avec la raison : l'outil n'étant pas distribué avec le contenu, il faut l'avoir installé séparément.
- **Statuts de plans corrigés** (étape 3.2) : `PLN-006` reçoit le champ `status` qui lui manquait (`exécuté`) ; `PLN-011` et `PLN-014` passent de statuts faux à `exécuté` ; `PLN-008` devient `remplacé par PLN-018 et PLN-019` ; `PLN-016` passe à `partiellement exécuté ; portée réduite par PLN-018 et PLN-019`, avec une entrée de changelog qui détaille ce qui lui reste.

### Implémentation (segment 1 de `PLN-019`)

- **Fichier réécrit** : `setup.sh`, v0.2.0 (34 lignes vers 215). Expose `install`, `--check`, `--uninstall`, `--contract-version`, `-h`, et conserve `. setup.sh activate`. Propriétés implémentées : bloc délimité par marqueurs explicites, idempotence réconciliante, vérification des dépendances et de l'accès en écriture **avant** toute écriture, écriture atomique par fichier temporaire puis remplacement, retrait exact, message d'activation.
- **Décision d'implémentation (mode strict conditionnel)** : `set -euo pipefail` n'est appliqué que lorsque le script est **exécuté**, jamais lorsqu'il est **sourcé**. Sourcer un script en mode strict altérerait le shell de l'appelant, ce que le mode `activate` rend inacceptable. Le script détecte son mode d'appel par comparaison de `BASH_SOURCE[0]` et `$0`.
- **Décision d'implémentation (testabilité sans crochet de test)** : le script lit `${HOME}/.bashrc`. Les scénarios redirigent `HOME` vers le bac à sable, ce qui évite d'ajouter au script une variable d'environnement dont le seul usage serait le test.
- **Fichier créé** : `test/test_setup.sh` (27 assertions, 9 scénarios). Couvre les flux nominaux et d'échec de `USE-001` : état initial, installation, préservation de la configuration préexistante, idempotence à racine identique, idempotence réconciliante à racine déplacée, unicité de la paire de marqueurs, retrait exact vérifié par comparaison octet à octet avec l'état d'origine, retrait sur configuration non installée, dépendance absente, configuration protégée en écriture, version du contrat, code 2 sur commande inconnue, et vérification finale que `HOME` n'a pas fui du bac à sable.
- **Correction pendant l'écriture des tests** : le scénario de dépendance absente échouait en code 127 plutôt qu'en code 1, parce qu'un `PATH` vidé privait le script de `env`, `bash` et des utilitaires dont il a besoin pour s'exécuter. Le bac à sable construit désormais un `PATH` minimal contenant ces utilitaires mais ni `yq` ni `git`, ce qui éprouve le contrôle de dépendances voulu et non un échec de démarrage.
- **Résultat des tests** : `test/test_setup.sh` **27 réussites, 0 échec**. `test/test_clia.sh` (préexistant) passe toujours, code 0.

## Résultats

- `PLN-018` v0.4.0, statut **`exécuté`** : trois ADR acceptés, deux exigences et deux spécifications produites ou amendées, rattachement amont refermé, `ACT-003` amendé, cinq statuts de plans corrigés.
- `PLN-019` v0.4.0, statut **`approuvé`** : segment 1 exécuté et éprouvé.
- **Arrêt au BREAKPOINT 1 de `PLN-019`.** Les segments 2 (créer un dépôt équipé), 3 (connaître les versions) et 4 (harnais cumulatif) ne sont pas exécutés. Le breakpoint existe parce que le segment 1 modifie la configuration de shell : il vous revient de l'éprouver sur votre poste avant que l'implémentation ne se poursuive. Statut `partial` du présent log pour cette raison.
- Ce que vous pouvez faire dès maintenant : `./setup.sh --check`, `./setup.sh install`, puis `source ~/.bashrc`, et `./setup.sh --uninstall` pour revenir en arrière.

## Commit message proposé

*Note: Ce message est fourni à titre informatif uniquement. C'est à l'humain de décider s'il doit être commité, rejeté, ou modifié. L'agent n'exécute jamais de git commit.*

```
PLN-018 execute, PLN-019 segment 1 : setup.sh install/check/uninstall

Surface : ADR-010 v0.3.0 retablit le groupe clia setup (sous-commandes init
et versions ; upgrade et downgrade reservees). ADR-013 et ADR-014 acceptes
tels quels.

Conception : REQ-003 et SPEC-004 (script d'amorcage et contrat d'extension
v1.0.0) ; REQ-002 v0.3.0 et SPEC-002 v0.3.0 amendes du groupe setup, avec la
table des quatre etats de reconnaissance d'une cible.

Tracabilite : USE-001 a USE-003 declarent desormais les exigences qui les
satisfont ; la chaine ACT vers USE vers REQ vers SPEC est fermee pour ces
trois parcours. ACT-003 couvre l'equipement du poste. Cinq statuts de plans
errones corriges (PLN-006, PLN-008, PLN-011, PLN-014, PLN-016).

Implementation : setup.sh v0.2.0 (install, --check, --uninstall,
--contract-version) avec bloc marque, idempotence reconciliante, controle
des dependances avant ecriture, ecriture atomique et retrait exact. Mode
strict applique en execution seulement, pour ne pas alterer le shell qui
source le script.

test/test_setup.sh : 27 assertions sur les 9 scenarios de USE-001, en bac a
sable (HOME redirige). 27 reussites, 0 echec. test_clia.sh passe toujours.

Arret au BREAKPOINT 1 de PLN-019.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

## Objections de l'agent

Aucune objection bloquante. Deux signalements :

- [SIGNALEMENT] `git` a été ajouté aux dépendances vérifiées par `setup.sh`, alors que la couche 1 seule ne s'en sert pas.
  - Impact : une installation échoue aujourd'hui sur une machine sans `git`, pour un besoin qui n'apparaîtra qu'au segment 2 (création du dépôt cible).
  - Suggestion : conserver, la dépendance étant réelle pour le parcours complet et le diagnostic précoce valant mieux qu'une panne en cours d'installation d'un dépôt. À reconsidérer si un usage de la seule couche 1 apparaît.
- [SIGNALEMENT] Le numéro de tâche du bloc traité est `42` dans `.dev/session.md`, alors que `42` désigne déjà la tâche précédente, et son titre est de niveau `#` au lieu de `##`.
  - Impact : le présent log est nommé `task-43` d'après votre demande, et ne correspond donc pas au numéro écrit dans le fichier de session.
  - Suggestion : renuméroter le bloc en `## 43. [feedback humain]`. `.dev/session.md` étant en édition humaine uniquement, l'agent n'y touche pas.

## Notes

- Les six positions du breakpoint 1 de `PLN-018` sont acceptées, sauf la surface de commandes. Les cinq autres (nom `install`, refus sur cible non vide, multi-utilisateur hors portée, marque comme marqueur de reconnaissance, copie par défaut) sont désormais gravées dans les ADR au statut `Accepté`.
- `setup.sh` implémente le contrat d'extension v1.0.0 et l'annonce par `--contract-version`, mais rien ne l'invoque encore : le mécanisme d'extension côté outil relève du segment 2.
- `CLAUDE.md` ignore toujours les types `ACT` et `USE` dans sa table des livrables (étape 2.3 de `PLN-017`, non exécutée).
- `BUG-007` reste ouvert : `clia res ls` et `clia --version --long` rapportent encore des données fausses. Sans effet sur ce qui a été livré ici, mais toujours sur le chemin des parcours de version.
