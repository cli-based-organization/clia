---
task: 18
session-date: 2026-07-09
status: completed
duration: 20 min
---

# Résumé de la tâche

Réviser `PLN-006` en intégrant les réponses de l'humain aux sept objections de la première version du plan (tâche 18, « Discussion des objections »).

## Travail effectué

Révision de `.dev/plans/PLN-006-cli-clia.md` (statut passé de `objection` à `proposé`) :

- **Objection 1** (`clia ses plan`) : la commande crée un squelette de session en planification `.dev/session-x<YZ>.md` à partir du template, `YZ` incrémenté de +1.
- **Objection 2** (sémantique `x<SEQ>`) : formalisation du cycle de vie de session (planification `x<YZ>` multiples / active unique / archivée), invariant global d'une seule session active, transitions par déplacement et renommage.
- **Objection 3** (fichiers humains) : `clia` est un CLI déterministe qui prend en charge les transitions d'état ; l'agent ne l'invoque jamais ; rôle à documenter dans `CONSTITUTION.md`.
- **Objection 4** (versions) : séparation des domaines de version ; `version.yaml` (racine) pour le métier, `.dev/ressources.yaml` pour les ensembles du système d'augmentation.
- **Objection 5** (horodatage) : date et heure d'ouverture/fermeture inscrites dans l'en-tête de session aux transitions.
- **Objection 6** (clia hors harnais) : modèle à trois composants (conception / harness-files / clia) ; correction d'`ADR-005` (l'ADR n'est pas du harnais ; le harnais implémente la conception) ; nouvel `ADR-007` d'architecture.
- **Objection 7** (arborescence) : suppression d'`activate` et `scripts/` ; `setup.sh` + `src/{bin,lib,...}` selon conventions Linux.

La liste des livrables passe de 7 à 10 (ajout : correction `ADR-005`, `ADR-007`, `version.yaml`). Trois objections résiduelles non bloquantes ajoutées avec défauts retenus (contenu de `--version`, emplacement de la version de `clia`, opportunité d'`ADR-007`).

Aucune exécution : tâche de planification uniquement.

## Résultats

- `PLN-006` révisé, statut `proposé`, en attente du feu vert d'exécution.

## Commit message proposé

*Note: Ce message est fourni à titre informatif uniquement. C'est à l'humain de décider s'il doit être commité, rejeté, ou modifié. L'agent n'exécute jamais de git commit.*

```
Reviser PLN-006 apres discussion des objections (tache 18)

Integration des reponses aux sept objections : cycle de vie de session,
role deterministe de clia, separation des versions (version.yaml),
modele a trois composants (correction ADR-005, ajout ADR-007),
arborescence src/ + setup.sh. Statut : proposé.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

## Objections de l'agent

Trois objections résiduelles **non bloquantes** (dans `PLN-006`), tranchées par défaut :
- OBJECTION-A : contenu exact de `clia --version` vs `--long`.
- OBJECTION-B : emplacement de la version propre de `clia`.
- OBJECTION-C : opportunité d'un `ADR-007` distinct.

Aucune objection bloquante ne subsiste : le plan est prêt à être exécuté sur décision de l'humain.

## Notes

- Livrables passés de 7 à 10 ; le plan reste un premier jet, amendable à l'exécution.
- Les deux domaines de versionnage (`version.yaml` métier vs `.dev/ressources.yaml` système) devront être cohérents avec `ADR-004`.
