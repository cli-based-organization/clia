---
type: log
task: 31
session-date: 2026-07-17
status: completed
duration: 25 min
---

# Résumé de la tâche

Traitement des objections humaines consignées à la tâche 31 de `session.md` sur le plan `PLN-016`. L'humain a répondu aux objections 2 à 6 de l'agent ; la tâche consiste à réviser `PLN-016` en conséquence (produire/réviser un plan est une tâche, `skl-003`).

## Travail effectué

- **Fichier révisé** : `.dev/plans/PLN-016-installation-cycle-de-vie-clia.md` (révision 3).
- **Correspondance objections** établie par le contenu (les numéros de session ne suivent pas strictement ceux du plan) :
  - session obj 2 -> PLN-016 obj 2 (surface de commandes) : retenir `clia setup <init|upgrade|downgrade>`, abandon du vocabulaire `init/update/rollback` ;
  - session obj 3 -> PLN-016 obj 3 (modes d'installation) : reproduire le `setup.sh` de `tda` (dev / permanent / local) en corrigeant ses fragilités selon `ANL-002` (idempotence réconciliante, marqueurs ouverture/fermeture, bash uniquement, noms synchronisés) ;
  - session obj 4 -> PLN-016 obj 4 (rollback) : pas de git ; `clia setup` ne modifie que les fichiers du système d'augmentation, le `downgrade` re-matérialise depuis l'arbre `clia` local ;
  - session obj 5 -> PLN-016 obj 6 (scripts externes) : contrat d'interface strict et versionné ;
  - PLN-016 obj 5 (source de distribution) : résolue par conséquence de l'obj 4 (arbre local uniquement).
- **Modifications** : frontmatter (`version` 0.1.0 -> 0.2.0, `status` objection -> résolu) ; ajout de l'entrée de changelog révision 3 ; mise à jour du tableau de réévaluation (lignes rollback et vocabulaire) ; réécriture des étapes 1.1, 1.3, du breakpoint et de 2.3 ; réécriture des objections 2 à 6 en RÉSOLUE avec les directives humaines.
- **Décision** : la correspondance obj 5 session -> obj 6 plan repose sur le contenu (« contrat strict et versionné d'interface » ne peut concerner la source de distribution). L'objection 5 du plan (source de distribution) n'a pas de résolution explicite mais découle de l'objection 4 (pas de git, seulement les fichiers d'augmentation => arbre local).
- **Point laissé ouvert (non bloquant)** : le livrable de session « créer un nouveau repo git » suppose un `git init` que la contrainte « pas de git » exclut de `clia setup` ; consigné comme question de conception à trancher dans l'ADR 1.1.

## Résultats

- `PLN-016` v0.2.0, statut `résolu` : aucune objection de l'agent ouverte ; l'approbation revient à l'humain.
- La surface de commandes, les modes d'installation, le mécanisme de `downgrade` sans git et le contrat d'extension sont désormais tranchés dans le plan.

## Commit message proposé

*Note: Ce message est fourni à titre informatif uniquement. C'est à l'humain de décider s'il doit être commité, rejeté, ou modifié. L'agent n'exécute jamais de git commit.*

```
PLN-016: résoudre les objections 2 à 6 (tâche 31)

Révision 3 de PLN-016 intégrant les directives humaines de la tâche 31 :
surface clia setup <init|upgrade|downgrade>, modes calqués sur tda corrigés
selon ANL-002, pas de git (downgrade par re-matérialisation locale),
distribution en arbre local, contrat d'interface strict et versionné.
Statut passé de objection à résolu (version 0.2.0).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

## Objections de l'agent

Aucune objection ouverte. Une question de conception non bloquante subsiste : la charge du `git init` pour un nouveau repo (livrable de session « créer un nouveau repo git »), `clia setup` ne faisant pas de git. À trancher dans l'ADR de conception (étape 1.1).

## Notes

Correspondance des numéros d'objection session/plan explicitée dans le changelog du plan et ci-dessus, car elle n'est pas biunivoque.
