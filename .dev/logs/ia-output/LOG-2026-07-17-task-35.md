---
type: log
task: 35
session-date: 2026-07-17
status: completed
duration: 25 min
---

# Résumé de la tâche

Tâche 35 de `session.md`, de type [traitement des objections] : prendre en compte les réponses humaines aux six objections de `PLN-017` et à l'objection générale de portée, puis raffiner le plan en conséquence.

## Travail effectué

- **Lecture préalable** : `.dev/session.md` en entier (tâches 34 et 35), `PLN-017` v0.1.0, `ANL-014` (constats, typologie d'acteurs, recommandations R1 à R7), `skl-003-plan-de-travail`, `skl-013-rapport-de-bogue`, `skl-008-log-ia-output`, `.dev/resource-types.yaml`, `PLN-016`, `ADR-010`.
- **Vérification de terrain avant d'écrire le bogue** : reproduction sur `src/bin/clia` des cinq écarts allégués par l'objection 1. Tous confirmés le 2026-07-29 : `res ls` affiche les cycles `travail` et `pointfixe` abolis ; `res ls PDC` retourne « type de ressource inconnu » avec code 2 ; `res ls FND` retourne une colonne version vide sur 18 fondations ; `res ls PLN` affiche des phrases entières de texte d'objection en guise d'état ; `--version --long` avertit de l'absence de `.dev/ressources.yaml`. Table codée en dur localisée en `src/lib/resource.sh` lignes 6 à 15.
- **Fichier créé** : `.dev/bugs/BUG-007-resource-sh-modele-abroge.md` (v0.1.0, statut `diagnostiqué`), en application de la résolution de l'objection 1 (« ouvrir un BUG et continuer sans autre modification »). Diagnostic : écart à `PDC-006` (table dupliquée au lieu d'être lue depuis `.dev/resource-types.yaml`) et à `PDC-009` (version déduite d'un nom de fichier au lieu du frontmatter) ; cause systémique rattachée à la même famille que `BUG-005`.
- **Fichier révisé** : `.dev/plans/PLN-017-cas-usage-acteurs-tracabilite.md`, v0.1.0 vers **v0.2.0**, statut `objection` vers **`résolu`**, titre resserré (« Cas d'usage, acteurs et traçabilité amont »).
- **Traitement des résolutions humaines** :
  - portée : R5 (tests d'acceptation) retirée, R4 réduite à son volet amont, ancienne étape 4.3 (mesure de couverture dans `clia`) supprimée. Le plan ne modifie plus aucun code ;
  - objection 1 : `BUG-007` ouvert, ancienne étape 4.0 (réconciliation de `src/lib/resource.sh`) supprimée ;
  - objection 2 : forme provisoire des relations retenue (relations écrites en liens markdown dans une section `## Relations` normalisée par les skills), dette nommée sur la couche relations lisible par un programme ;
  - objection 3 : ajout de l'étape 3.3 (révision de `PLN-016` pour qu'il déclare les `USE` qu'il réalise) ; la collision sur les harnais de test est déclarée caduque, les tests sortant du plan ;
  - objection 4 : règle d'indépendance des ressources aux outils gravée dans le contexte et dans l'ADR du type `USE` ; gabarit purgé des codes de retour ; les trois `USE` du parcours d'installation réintègrent le catalogue initial et la dépendance à `ADR-010` tombe ;
  - objection 5 : création d'un type de ressource autonome `ACT` (acteur), avec son ADR, son skill, son entrée de couche type et son répertoire `.dev/acteurs` ;
  - objection 6 : toutes les étapes touchant `ARCHITECTURE.md` supprimées.
- **Décision (deux ADR et deux skills plutôt qu'un)** : la création d'un type de ressource autonome `ACT` suit la convention du dépôt, un ADR par type de ressource (`ADR-001` analyse, `ADR-003` bogue, `ADR-008` principe, `ADR-009` harnais `ARCHITECTURE.md`). Le plan prévoit donc `ADR-011` (acteur), `ADR-012` (cas d'usage), `skl-016-acteur` et `skl-017-cas-d-usage`. La numérotation retenue place l'acteur avant l'usage, l'usage citant l'acteur.
- **Décision (relations demandées à la tâche 34, objection C)** : les deux énoncés « acteur utilise usage pour but » et « fonctionnalité satisfait usage d'acteur » sont transcrits en deux relations ajoutées au vocabulaire, `utilise` et `satisfait`. Aucun type `FONCTIONNALITÉ` n'est créé : le `REQ` porte déjà cette fonction.
- **Décision (forme)** : application de la convention de références croisées de la tâche 34 (objection B) au plan révisé et au bogue produit ; toute citation d'un autre document porte un lien markdown relatif, avec ancre de section quand la section est identifiable.
- **Dette consignée sans objection** : `ARCHITECTURE.md` conserve une section « Acteurs et rôles » qui coexistera avec le catalogue `ACT`. Le point est inscrit au contexte du plan comme dette assumée, l'humain ayant explicitement placé ce fichier hors portée.

## Résultats

- `.dev/bugs/BUG-007-resource-sh-modele-abroge.md` v0.1.0, statut `diagnostiqué` : cinq écarts reproduits et documentés, correctif prévu en quatre points, critères de vérification concrets, correction explicitement hors portée de `PLN-017`.
- `.dev/plans/PLN-017-cas-usage-acteurs-tracabilite.md` v0.2.0, statut **`résolu`** : quatre segments, deux breakpoints, onze étapes, **aucune objection ouverte**. Le plan produit deux types de ressources (`ACT`, `USE`), deux ADR, deux skills, deux catalogues et le rattachement amont des exigences, sans toucher au code, à `ARCHITECTURE.md` ni aux tests.
- L'approbation du plan revient à l'humain (`CONSTITUTION.md`).

## Commit message proposé

*Note: Ce message est fourni à titre informatif uniquement. C'est à l'humain de décider s'il doit être commité, rejeté, ou modifié. L'agent n'exécute jamais de git commit.*

```
PLN-017 v0.2.0 : resolution des objections, portee resserree

Traitement des resolutions humaines de la tache 35. Les six objections de
l'agent sont levees et le plan passe au statut resolu.

Portee : les tests et la mesure de couverture sortent du plan (R5 retiree,
R4 reduite a son volet amont) ; aucun code n'est modifie ; ARCHITECTURE.md
est hors portee.

Modele : les acteurs deviennent un type de ressource autonome ACT (au lieu
d'une section de ARCHITECTURE.md), avec son ADR et son skill ; une ressource
est declaree independante des outils qui la manipulent, ce qui detache le
cas d'usage d'installation de ADR-010 et le reintegre au catalogue initial ;
les relations adoptent une forme provisoire assumee, avec dette nommee.

Ouvre BUG-007 : src/lib/resource.sh expose la table de types d'ADR-004
v0.1.0, abrogee par la v0.2.0 (cycles abolis, PDC inconnu, versions FND
vides, etat PLN errone, manifeste supprime encore recherche).

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

## Objections de l'agent

Aucune. Les six objections de la révision 1 de `PLN-017` sont résolues.

## Notes

- Les numéros de ressources cités par le plan (`ADR-011`, `ADR-012`, `skl-016`, `skl-017`) sont les prochains libres constatés au 2026-07-29. La séquence des skills comporte des trous (`skl-005` et `skl-007` absents) ; le plan n'y touche pas.
- Les répertoires `.dev/acteurs/` et `.dev/usages/` n'existent pas encore ; ils seraient créés au segment 3, après le premier breakpoint.
- `BUG-007` est un document vivant : son statut évoluera vers `résolu` puis `fermé` quand la phase d'implémentation traitera l'écart.
