---
task: 02
session-date: 2026-07-10
status: partial
duration: ~30 min
---

# Résumé de la tâche

Tâche 2 (catégorie préparer) de la session du 2026-07-10 : préparer l'implémentation des fonctionnalités d'installation de `clia`. Le livrable est un plan de travail (`skl-003`) préparant l'implémentation, pas l'implémentation elle-même. Statut `partial` car le plan porte une objection d'agent ouverte (décision de modèle de racine) à trancher par l'humain avant exécution.

## Travail effectué

- **Cadrage du type** : catégorie `[préparer]` -> livrable = plan de travail (`PLN`), à distinguer de l'exécution. L'implémentation fera l'objet d'une tâche ultérieure (« exécuter PLN-008 »).
- **État des lieux de l'existant** : `setup.sh` de `clia` (30 lignes) n'est qu'un activateur in-repo (`. setup.sh activate`, PATH du shell courant, non persistant, sans `--check`/`--uninstall`). `src/bin/clia` résout `REPO_ROOT` depuis l'emplacement du binaire (`REQ-002-NF2`).
- **Références mobilisées** : `ANL-2026-07-10-setup-installation` (tâche 1, socle « dev + permanent + local » de `tda` + 5 recommandations), `SPEC-001`/`REQ-001` (convention CLI bash), `SPEC-002`/`REQ-002` (dont `NF2` racine via `BASH_SOURCE` et `NF5` activation), `ADR-004`/`ADR-007` (versionnage de l'ensemble `clia`).
- **Identification d'une décision de conception** : une installation persistante globale ferait opérer `clia` sur son propre dépôt d'installation (racine via `BASH_SOURCE`), pas sur le dépôt de l'utilisateur. Formalisée en objection A (modèle A dev/mono-dépôt vs modèle B outil global + `-C`/cwd).
- **Intégration du contexte de session** : les « tests fragmentés qui ne se cumulent pas » sont adressés par une étape dédiée du plan (harnais de test d'installation reproductible et cumulatif en bac à sable isolé).

Fichiers créés (chemins relatifs) :
- `.dev/plans/PLN-008-installation-clia.md`
- `logs/ia-output/LOG-2026-07-10-task-02.md`

## Résultats

- `PLN-008-installation-clia.md` : plan en 6 étapes (installeur complet conforme `SPEC-001` ; correction des fragilités de l'ANL ; périmètre de shell ; conformité `REQ-001` ; harnais de test cumulatif ; versionnage), 3 décisions de premier jet, 2 objections d'agent (A modèle de racine, à trancher ; B couplage au profil, non bloquant sous conditions).
- Statut du plan : `proposé`, une objection ouverte (A). Aucune implémentation réalisée (conforme à `[préparer]`).

## Commit message proposé

*Note: Ce message est fourni à titre informatif uniquement. C'est à l'humain de décider s'il doit être commité, rejeté, ou modifié. L'agent n'exécute jamais de git commit.*

```
Préparer l'installation de clia (PLN-008)

Tâche 2 (préparer) : plan de travail pour doter clia de vraies
fonctionnalités d'installation (locale, permanente, réversible), à partir
du socle analysé en tâche 1 (ANL-2026-07-10-setup-installation).

- PLN-008 : installeur complet conforme SPEC-001, correction des fragilités
  relevées (idempotence réconciliante, désinstallation déterministe),
  harnais de test cumulatif, versionnage.
- Objection ouverte A : modèle de racine (dev/mono-dépôt vs outil global
  + -C/cwd) à trancher par l'humain avant exécution.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

## Objections de l'agent

Report des objections portées par `PLN-008` (détail dans le plan) :

- [OBJECTION] Modèle de racine à trancher (A dev/mono-dépôt vs B outil global + `-C`/cwd).
  - Impact : détermine la nature de l'installation et un éventuel amendement de `REQ-002-NF2`/`SPEC-002`.
  - Suggestion : décision humaine via `session.md` avant exécution ; à défaut, exécution du modèle A (le moins risqué, aligné sur l'existant et sur `tda`).
- [OBJECTION] Écriture dans un fichier de profil (`~/.bashrc`) hors dépôt.
  - Impact : risque de corruption du profil si mal borné.
  - Suggestion : non bloquant si marqueurs bornés (ouverture/fermeture) et tests en `$HOME` factice.

## Notes

- Tâche de préparation : aucun code produit, aucune modification hors des livrables de conception. `setup.sh` et `src/` de `clia` n'ont pas été modifiés.
- `PLN` et `LOG` sont des ressources non versionnées (`ADR-004`) : aucun bump de `.dev/ressources.yaml` à ce stade ; le bump interviendra à l'exécution de `PLN-008`.
