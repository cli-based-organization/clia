---
task: 30
session-date: 2026-07-17
status: partial
duration: ~35 min
---

# Résumé de la tâche

Tâche 30 de `.dev/session.md` : « [exécution du plan] Exécuter PLN-014 et PLN-015. Puis réviser PLN-016 ». Exécution du chantier de refonte des ressources (`PLN-014`), de la commande `clia release` (`PLN-015`), puis révision du plan d'installation (`PLN-016`).

## Travail effectué

`PLN-014` contient un **breakpoint** qu'il impose lui-même : l'humain valide le nouveau modèle (réécriture d'`ADR-004` + schéma de `type`) **avant** la migration mécanique de masse (frontmatter sur environ 90 fichiers, renommage de ~27 fichiers `FND`/`ANL` avec réparation de toutes les références croisées, rapatriement des `logs`, suppression de `ressources.yaml`). Cette migration est fortement couplée et difficilement réversible. Exécuter `PLN-014` « tel qu'écrit » signifie donc produire le **Segment 1** puis s'arrêter au breakpoint. J'ai exécuté le Segment 1 et je me suis arrêté ; `PLN-015` et la révision de `PLN-016` sont tenus pour la reprise après breakpoint, afin qu'ils atterrissent dans le modèle stabilisé.

Segment 1 de `PLN-014` exécuté :

1. **Réécriture d'`ADR-004`** (`.dev/adr/ADR-004-ressources-livrables.md`, version 0.1.0 -> 0.2.0) au format **frontmatter** (exemplaire du nouveau modèle). Grave le modèle décidé : une seule catégorie de **ressources livrables vivantes** versionnées en frontmatter ; **traces immuables** (logs, sessions) hors ressources livrables ; **zones** (`@.`, `@.dev`, `doc` inchangé, pas de `.knowledge`, `logs` rapatriés dans `.dev`) ; **frontmatter** remplaçant les puces ; **abolition de `ressources.yaml`** (version en frontmatter) ; **nommage séquencé** pour les livrables, horodaté pour les traces ; **références croisées** en liens avec vocabulaire de relations, validables par `clia`.
2. **Schéma de la couche type** (`.dev/resource-types.yaml`) : source de vérité machine-lisible de l'ontologie légère explicite (`ANL-2026-07-21`). Décrit chaque type (préfixe, emplacement, nommage, skill producteur, droits d'édition), les traces, les zones, les fichiers exemptés (édition humaine), le schéma de frontmatter et le vocabulaire de relations. Remplace l'ancien manifeste `ressources.yaml` comme source de la couche type.

Mise à jour de `PLN-014` : statut « approuvé (Segment 1 exécuté, au breakpoint) », entrée de changelog d'exécution.

Fichiers créés : `.dev/resource-types.yaml`, ce log. Modifiés : `.dev/adr/ADR-004-ressources-livrables.md` (réécriture), `.dev/plans/PLN-014-adoption-conventions-okf.md` (statut/changelog).

## Décisions prises

- **Respect du breakpoint** : arrêt après le Segment 1, avant la migration mécanique de masse. C'est le seul chemin qui honore à la fois l'instruction d'exécuter et le breakpoint inscrit dans le plan (mécanisme de gouvernance, `CONSTITUTION.md`).
- **`PLN-015` et révision de `PLN-016` différés** après le breakpoint, pour deux raisons : ils dépendent du modèle stabilisé (versionnage en frontmatter, abolition du manifeste), et `PLN-015` amende `SPEC-002`/`REQ-002` dont le format changera au Segment 2 (éviter la double manipulation et l'incohérence transitoire).
- **`ADR-004` porte lui-même le frontmatter** (nouveau format) pour servir d'exemple, bien que la migration générale des autres fichiers soit au Segment 2. Interim assumé : l'existant coexiste en deux formats et `ressources.yaml` liste encore `ADR-004` en 0.1.0 jusqu'à sa suppression au Segment 2.

## Résultats

- Le nouveau modèle de ressources est **conçu et gravé** (`ADR-004` 0.2.0 + `.dev/resource-types.yaml`), prêt pour revue humaine.
- Exécution suspendue au breakpoint : aucune migration mécanique, aucun renommage, aucune suppression de `ressources.yaml`, aucun amendement de skill n'a été réalisé.
- `PLN-015` (commande `clia release`) et la révision de `PLN-016` restent à faire après autorisation de reprise.

## Commit message proposé

*Note: Ce message est fourni à titre informatif uniquement. C'est à l'humain de décider s'il doit être commité, rejeté, ou modifié. L'agent n'exécute jamais de git commit.*

```
execution: PLN-014 Segment 1 (reecriture ADR-004 + schema de types), au breakpoint

Grave le modele unifie des ressources (ADR-004 0.2.0, format frontmatter) :
une categorie de ressources livrables vivantes versionnees en frontmatter,
traces immuables, zones (.dev), abolition de ressources.yaml, nommage sequence,
references en liens. Cree .dev/resource-types.yaml (couche type machine-lisible,
ontologie legere). Arret au breakpoint avant la migration de masse. PLN-015 et
revision de PLN-016 differes apres revue humaine.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

## Objections de l'agent (le cas échéant)

- [OBJECTION - breakpoint] Le Segment 2 de `PLN-014` (migration mécanique de masse) est difficilement réversible (renommage de ~27 fichiers et réparation des références croisées à travers le dépôt, suppression de `ressources.yaml` que `clia` lit encore). Je me suis arrêté au breakpoint pour revue.
  - Impact : risque de références cassées et d'incohérences si la migration est menée sans revue du modèle.
  - Suggestion : valider `ADR-004` 0.2.0 et `.dev/resource-types.yaml`, puis autoriser la reprise (Segment 2), après quoi j'enchaînerai `PLN-015` et la révision de `PLN-016`.
- [OBJECTION - conséquence sur `clia`] L'abolition de `ressources.yaml` (Segment 2) cassera `clia --version --long` et `clia res ls` qui le lisent. Leur réconciliation (lecture des versions en frontmatter) est un chantier `clia` distinct à planifier.

## Notes

- Ordre de reprise proposé après validation : Segment 2 de `PLN-014` (migration) -> `PLN-015` (`clia release`) -> révision de `PLN-016`.
- Le modèle gravé opérationnalise les recommandations de `ANL-2026-07-21` (expliciter l'ontologie légère, couche type machine-lisible, relations typées, rester léger sans RDF/OWL).
- Conformément à la gouvernance, aucune action git n'a été réalisée ni proposée comme directive ; le message ci-dessus est purement documentaire.
